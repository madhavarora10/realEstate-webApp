// import {
//   getAccessToken,
// } from '@auth0/nextjs-auth0';
export {};
// import {
//   ApiRequest,
//   ApiResponse,
// } from '../../../interfaces';

// const withHeaders = (handler: any) => async (req: ApiRequest, res: ApiResponse) => {
//   const { accessToken } = await getAccessToken(req, res);

//   const newHeaders = {
//     authorization: `Bearer ${accessToken}`,
//   };

//   req.customHeaders = newHeaders;

//   return handler(req, res);
// };

// export default withHeaders;
