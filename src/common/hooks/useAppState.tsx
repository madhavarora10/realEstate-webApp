export {};
// import { useContext } from 'react';

// import { AppStateContextType } from '../types';
// import { AppStateContext } from '../providers/AppStateProvider';

// export const useAppState = () => (useContext(AppStateContext)) as AppStateContextType;
